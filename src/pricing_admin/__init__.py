# TODO: Not yet deployed — no aws_lambda_function Terraform resource exists.
# API Gateway routes reference this via input variables (admin_api module)
# but the Lambda itself is not created. See GitHub issue #55.
