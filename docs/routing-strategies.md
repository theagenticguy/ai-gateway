# Provider Routing Strategies

Available strategies: single (default), fallback, loadbalance.
See infrastructure/portkey-configs/ for pre-built templates.
Pass configs via x-portkey-config header (base64-encoded JSON).
